// Test stub for the `server-only` package, which throws when imported outside a React Server
// Component. Aliased in vitest.config.ts so server-side modules (e.g. lib/weather.ts) can be
// unit-tested in the node environment.
export {};
